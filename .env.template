# ═══════════════════════════════════════════════════════════════
# Dualign — 本地构建环境配置
# ═══════════════════════════════════════════════════════════════
# 将本文件复制为 .env，填入你本机的实际路径。
# .env 已被 .gitignore 排除，不会提交到仓库。
# ═══════════════════════════════════════════════════════════════

# ── Inno Setup 编译器路径 ──
# 用于编译安装包 (python scripts/build.py)
# 留空则脚本自动在常见路径中查找
ISCC_PATH=

# ── PyInstaller 入口文件 ──
# 通常无需修改，除非入口文件位置有变动
# ENTRY_POINT=src/dualign/__main__.py
